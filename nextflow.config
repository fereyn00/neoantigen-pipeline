params {
    netmhcpan_chunk_size = 2000
    netmhcpan_max_forks = 4
}

process {
    executor = 'local'

    withName: NETMHCPAN {
        maxForks = params.netmhcpan_max_forks
    }
}
docker {
    enabled = true
}
